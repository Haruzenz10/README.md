const { execSync } = require("child_process");
const msg = process.argv.slice(2).join(" ") || "update";
["git add .", `git commit -m "${msg}"`, "git push"].forEach(cmd => execSync(cmd, { stdio: "inherit" }));
node push.js fix bug
